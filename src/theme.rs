use iced::Color;

// ── Omarchy Colour Palette ──
pub const BG: Color = Color::from_rgb(
    0x0c as f32 / 255.0,
    0x0b as f32 / 255.0,
    0x0c as f32 / 255.0,
);
pub const SURFACE: Color = Color::from_rgb(
    0x16 as f32 / 255.0,
    0x15 as f32 / 255.0,
    0x16 as f32 / 255.0,
);
pub const FG: Color = Color::from_rgb(
    0xFA as f32 / 255.0,
    0xFC as f32 / 255.0,
    0xFB as f32 / 255.0,
);
pub const ACCENT: Color = Color::from_rgb(
    0xb5 as f32 / 255.0,
    0x97 as f32 / 255.0,
    0x90 as f32 / 255.0,
);
pub const DIM_TEXT: Color = Color::from_rgb(
    0x88 as f32 / 255.0,
    0x88 as f32 / 255.0,
    0x88 as f32 / 255.0,
);
pub const HEADER_FG: Color = Color::from_rgb(
    0xFF as f32 / 255.0,
    0xFF as f32 / 255.0,
    0xFF as f32 / 255.0,
);
pub const SUCCESS: Color = Color::from_rgb(
    0x4a as f32 / 255.0,
    0xcc as f32 / 255.0,
    0x7a as f32 / 255.0,
);
pub const ERROR: Color = Color::from_rgb(
    0xef as f32 / 255.0,
    0x44 as f32 / 255.0,
    0x44 as f32 / 255.0,
);
pub const BORDER: Color = Color::from_rgb(
    0x2a as f32 / 255.0,
    0x2a as f32 / 255.0,
    0x2a as f32 / 255.0,
);
pub const BORDER_SUBTLE: Color = Color::from_rgb(
    0x22 as f32 / 255.0,
    0x22 as f32 / 255.0,
    0x22 as f32 / 255.0,
);

// ── Layout Spacing ──
pub const SPACING: f32 = 8.0;
pub const SPACING_SM: f32 = 4.0;
pub const SPACING_LG: f32 = 16.0;
pub const PADDING: f32 = 12.0;
pub const PADDING_LG: f32 = 16.0;
pub const CORNER_RADIUS: f32 = 6.0;
pub const BORDER_WIDTH: f32 = 1.0;

// ── Typography ──
pub const TEXT_XS: f32 = 11.0;
pub const TEXT_SM: f32 = 12.0;
pub const TEXT_MD: f32 = 13.0;
pub const TEXT_LG: f32 = 14.0;
pub const TEXT_XL: f32 = 16.0;
